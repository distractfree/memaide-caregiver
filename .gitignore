.env
__pycache__/
*.egg-info/
.venv/
venv/
.pytest_cache/
# Eval runs are archived (and tracked) under docs/eval-runs/<run_id>/.

# Per-session frame recordings (FileSessionRecorder output)
recordings/

# Live-preview snapshot dir (run_bridge_server: index.html + latest.jpg/json)
preview/
